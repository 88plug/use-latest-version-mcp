# Changelog

This page mirrors [`CHANGELOG.md`](https://github.com/88plug/use-latest-version-mcp/blob/main/CHANGELOG.md)
in the repository root. The plugin itself is versioned CalVer
(`YEAR.MONTH.BUILD`), auto-stamped from the commit count by the 88plug
marketplace, so every push is a uniquely identifiable release.
