node_modules/
build/
dist/
*.log
.DS_Store
.env
*.tgz

# one-off dev/maintenance scripts (kept locally, not part of the package)
fix_*.cjs
update_*.cjs

# Claude Code session/worktree scratch (ephemeral, never part of the package)
.claude/

# Test scratch — these dirs are (re)generated by the test suite on every run, so
# they must never be tracked (otherwise running tests dirties the working tree).
test-dependency-files/
test-outdated-check/
test-project-scan/
test-upgrade-applier-temp/
test-temp-*/
.dependency-backups/

# MkDocs build output
site/

# real eval/corpus data — never commit
eval_real_corpus.json
.real_corpus_manifest.json

# round-specific campaign sweep scripts (one-time artifacts)
scripts/sweep_r*.py

# eval outputs
eval_run_*.json
EVAL_REPORT.md
